# Sample Java Project

A sample Spring Boot application with user management.

## Quick Start

```bash
mvn spring-boot:run
```

## API Endpoints

- GET /api/users - List all users
- GET /api/users/{id} - Get user by ID
- POST /api/users - Create a new user
